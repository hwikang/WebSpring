<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace ="kr.goott.web.member.MemberDAOInterface">
	<select id="logCheck" resultType="kr.goott.web.member.MemberVO"> 									<!-- vo를 넣어줬기때문에 거기서 꺼냄 -->
		select userid,userpwd,username from register where userid=#{userid} and userpwd=#{userpwd}
	
	</select>
</mapper>